#ifndef Car_h
#define Car_h
#include "Vehicle.h"

class Car : public Vehicle {
private:


public:
	void random_dimensions();	//Used to randomly generate a Car within a threshold
	// of sizes

};
#endif // !CAR_H